{
  "name": "my-sst-app",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "test": "sst test",
    "start": "sst start",
    "build": "sst build",
    "deploy": "sst deploy",
    "remove": "sst remove"
  },
  "devDependencies": {
    "@aws-cdk/assert": "1.94.1"
  },
  "dependencies": {
    "@aws-cdk/core": "1.94.1",
    "@serverless-stack/cli": "0.10.3",
    "@serverless-stack/resources": "0.10.3",
    "aws-sdk": "^2.869.0",
    "ksuid": "^2.0.0"
  }
}
